@extends('layouts.app')
@section('content')
<div class="app-shell more-page-compact"><header class="topbar"><div><p class="eyebrow">PENGELOLAAN</p><h1>Kategori</h1></div><a class="avatar avatar-link" href="{{ route('more') }}">⌂</a></header><main class="content"><section class="other-section category-section"><div class="section-heading"><div><p class="eyebrow">KATEGORI AKTIF</p><h2>{{ $categories->count() }} kategori</h2></div><button class="icon-button" type="button" aria-label="Tambah kategori" onclick="document.querySelector('#category-sheet').showModal()">+</button></div><div class="category-manage-list">@forelse($categories as $category)<div class="category-manage-row"><a href="{{ route('categories.edit', $category) }}"><span class="category-swatch" style="background: {{ $category->color }}"></span><div><strong>{{ $category->name }}</strong><small>{{ $category->type === 'income' ? 'Pemasukan' : 'Pengeluaran' }}</small></div></a><form method="POST" action="{{ route('categories.destroy', $category) }}" onsubmit="return confirm('Hapus kategori {{ addslashes($category->name) }}?')">@csrf @method('DELETE')<button type="submit">Hapus</button></form></div>@empty<span class="muted">Belum ada kategori.</span>@endforelse</div></section></main><nav class="bottom-nav"><a href="{{ route('dashboard') }}"><span>⌂</span>Home</a><a href="{{ route('reports') }}"><span>◒</span>Laporan</a><a class="nav-plus" href="{{ route('dashboard') }}#transaction-sheet">+</a><a href="{{ route('goals') }}"><span>◎</span>Target</a><a class="active" href="{{ route('more') }}"><span>•••</span>Pengaturan</a></nav></div>
<dialog id="category-sheet" class="sheet"><form method="POST" action="{{ route('categories.store') }}" class="sheet-content">@csrf<button type="button" class="sheet-close" onclick="this.closest('dialog').close()">×</button><p class="eyebrow">KATEGORI BARU</p><h2>Tambah kategori</h2><label>Nama kategori<input name="name" maxlength="80" required placeholder="Contoh: Kesehatan"></label><label>Jenis<select name="type"><option value="expense">Pengeluaran</option><option value="income">Pemasukan</option></select></label><label>Warna<input type="color" name="color" value="#126E73" required></label><button class="button button-primary" type="submit">Simpan kategori</button></form></dialog>
@endsection
